import { EspecialidadHorarios } from './especialidad-horarios';

describe('EspecialidadHorarios', () => {
  it('should create an instance', () => {
    expect(new EspecialidadHorarios()).toBeTruthy();
  });
});
